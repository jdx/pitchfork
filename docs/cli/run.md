# `pitchfork run`

- **Usage**: `pitchfork run [-f --force] <ID> [RUN]...`
- **Aliases**: `r`

Runs a one-off daemon

## Arguments

### `<ID>`

Name of the daemon to run

### `[RUN]...`

Runs a one-off daemon

## Flags

### `-f --force`

Runs a one-off daemon
