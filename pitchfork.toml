#:schema https://pitchfork.en.dev/schema.json

[daemons.docs]
run = "mise run -q docs"
ready_output = "http://localhost:"
port = { expect = [5173], bump = 10 }
pty = true

[settings.proxy]
enable = true
# Agents prefer non-sudo
https = false